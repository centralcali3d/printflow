import { ScreenScaffold } from '@/components/screen-scaffold';

export default function StockScreen() {
  return (
    <ScreenScaffold
      title="Stock"
      subtitle="Products, inventory, filament, and supplies. Filament is priced per lot from its real spool weight, so two colours of one type can cost different amounts."
      arrivesIn="Tasks 3.3-3.7"
    />
  );
}
