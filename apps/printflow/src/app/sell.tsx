import { ScreenScaffold } from '@/components/screen-scaffold';

export default function SellScreen() {
  return (
    <ScreenScaffold
      title="Sell"
      subtitle="Record a sale with a live payout, cost, and profit preview before saving — and an unmissable warning when the sale loses money. Recording a sale decrements inventory automatically."
      arrivesIn="Task 3.8"
    />
  );
}
