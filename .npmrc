# Metro (React Native's bundler) does not follow pnpm's symlinked node_modules
# reliably, so Expo requires a hoisted layout. Without this, the app builds
# locally in ways that fail unpredictably on module resolution.
# https://docs.expo.dev/guides/monorepos/
node-linker=hoisted
